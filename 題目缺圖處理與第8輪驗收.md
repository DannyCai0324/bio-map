# 題目缺圖問題處理報告 ＋ 第 8 輪插圖驗收

## 一、緊急：83 題的題幹提到圖，但題庫裡一張圖都沒有

掃過全部 815 題，題幹出現「下圖／如圖／圖 N／圖中／圖示」等字樣的有 **83 題**，
其中**有附圖的：0 題**。題目資料雖然有 `figure` 欄位、程式也支援渲染，但沒有任何一題填了值。
學生在課堂上遇到的就是這個——題目叫他看圖，畫面上沒有圖。

順帶查到：資料裡有 `pending`（33 題）與 `needs_review`（59 題）兩個旗標，
但程式從頭到尾沒有讀它們，所以標了也照樣出題。

### 分類與處置

逐題實際試答之後分成三類：

| 類別 | 題數 | 意思 | 處置 |
| --- | --- | --- | --- |
| **BLOCKED** | 11 | 關鍵資訊只在圖上，且從解析也還原不出來（有些選項本身就是四張圖） | **移出出題池**；建議之後直接刪除或改寫 |
| **PENDING_FIG** | 36 | 沒有圖不能作答，但從題幹＋解析可以重建那張圖 | **先移出出題池**，列為補圖清單 |
| **OK** | 36 | 圖只是把文字畫出來，題幹已含作答所需全部資訊 | 留在池內，**題幹下方加一行提示** |

**出題池 815 → 768 題**（移除 47 題）。錯題本裡若有這些題也一併濾掉。

處置名單放在新檔 **`qfigs.json`**，`index.html` 載入後套用——
跟 `figures.json`／`mixups.json` 一樣是外掛檔，之後補完一張圖就從名單移掉一個 id 即可，
不必動 3.3 MB 的 `index.html`。

### 順手做的兩件事

1. **題目附圖現在支援直接內嵌 SVG**。原本只吃 URL 或 `data:` base64（用 `<img>`），
   現在如果值是 `<svg …>` 開頭就直接嵌入並自動套用樣式隔離。
   也就是說，補圖可以直接沿用 `figures.json` 那一套畫法與規格，不必先轉成圖檔。
2. **`figure` 可以是單張（`figure`）或多張（`figures`）**，行為不變。

### BLOCKED 的 11 題（建議刪除或改寫）

`K11132`、`K11226`、`Z10031`、`Z10432`、`Z10510`、`Z10544`、
`Z10732`、`Z10743`、`Z10908`、`Z11030`、`Z11047`

其中 **`Z10510` 是我自己覆核加進來的**——它的選項混進了原卷圖上的文字
（例如「酵素甲活性不因酸鹼度變化而有影酵酵素素甲 ① 甲 ② ③ ④ ⑤ 響活活性性乙乙 ② ④ ⑤」），
解析也標著【待核】。這不只是缺圖，是 OCR 汙染。

另外掃了全題庫的 OCR 疊字污染，只有 5 題，而且 4 題本來就在上面的名單裡，
沒有擴散到其他地方。

### PENDING_FIG 的 36 題：補圖清單

這些圖大多不難畫，而且畫出來對整份題庫的價值很高。依主題分組：

- **曲線／數據圖**：`QF001` `Z10616`（動作電位曲線標甲乙丙丁）、`Z10528`（酵素飽和曲線）、
  `Z10729`（動脈→微血管→靜脈的血壓/流速曲線）、`Z10835`（HIV 十年三條曲線）、
  `K11331`（三種色素的吸收光譜）、`K11130` `K11234` `K11227` `Z10722` `Z10723` `Z10741` `Z11028`（長條圖類）
- **構造標號圖**：`Z10632`（葉綠體＋粒線體標甲～戊）、`Z10806`（心臟剖面標甲～庚）、
  `Z11046`（腎臟剖面）、`Z10607`（心肌細胞）、`Z10613`（血液抹片）、
  `Z10707`（C₄ 花環構造）、`K11429`（葉橫切氣孔位置）、`K11329`（質壁分離）、
  `K11412`（DNA 局部標甲乙丙）、`Z10818`（雙重受精標甲乙丙丁）、`Z10727`（核糖體 A/P/E 位）
- **流程／關係圖**：`Z10713`（下視丘→腦垂腺→標的腺負回饋）、`Z10730`（神經元匯聚）、
  `Z10833` `Z10834`（原核邊轉錄邊轉譯）、`Z10647` `Z11035`（食物網）、
  `K11326` `K11328`（孢粉圖）
- **實驗結果圖**：`Z10621`（電泳四泳道）、`K11312` `Z11026`（核型圖）、`K11408`（三管細胞形態）

**這批不急著全部做完。** 建議的順序是：先做「同一張圖可以救兩題」的
（`Z10833`＋`Z10834`、`K11326`＋`K11328`、`Z10722`＋`Z10723`、`QF001`＋`Z10616`），
一張圖解鎖兩題，投報率最高。

---

## 二、第 8 輪插圖驗收（`d9750ef` ＋ `cd21fa9`）

**張數對得上**：106 張／28 課，T5-P4～P7 各 4 張。`figures.json` 的排版格式（`indent=1`）正常。

**規格檢查**：16 張新圖零違規——沒有 hex 色、沒有 `rgb(`、沒有 `<image>`／`foreignObject`／
`url(#)`／`<use>`／`id=`、沒有非等比縮放、`viewBox` 寬度一律 640、
根 `<svg>` 沒有 `width`／`height`、`role` 與 `aria-label` 齊全。

**規則 F**：新圖 **16 張零項目**，與你的回報一致。目前全庫 70 項全部落在 21 張舊圖上，
就是第 3 輪報告列的那批待清理清單。

### 但有一處新缺陷，兩支檢查都抓不到

**第 92 張（T5-P4 @5，雙重受精）的「精①」「精②」用了 `class="r"`，但那張圖沒有定義 `.r`。**

那兩個 `<text>` 因此掉回 SVG 預設值——**黑色、預設字級、無字重**。
我把它渲染出來確認過：在暗色主題下，這兩個標籤幾乎融進深色背景看不見，
而它們是整張圖最關鍵的兩個標示。

這正是你自己抓到的 `.aw` 那個問題的同一類，只是這次漏掉了。

**建議改法**：把 `class="r"` 改成該圖已有的引線標籤類別（`.l`），
或在 `<style>` 裡補一條 `.r` 的定義。

### 所以我把這個檢查加進 `figcheck.js` 了

新增一段靜態檢查：**用到的 class 一定要有定義，或該元素自帶 `fill`／`stroke` 等呈現屬性。**
它會逐張列出沒有規則可套的 class，並說明「會掉回 SVG 預設值（黑色／無線條）」。

實測：全庫 106 張只抓出第 92 張那兩處，沒有誤報
（第 48 張的 `.bx` 雖然也沒定義，但它自帶 `fill`／`stroke` 屬性，正確排除）。

**同時照你的建議把路徑改成吃環境變數：**

```bash
FIGCHECK_PW=/path/to/playwright FIGCHECK_CHROME=/path/to/chrome node figcheck.js
```

兩個都不設就先試一般的 `require('playwright')`、用 playwright 內建的瀏覽器。
你那支 `figcheck-win.js` 包裝層可以退休了。

---

## 三、回覆你問的三件事

**1. 第 7 輪那兩處＋內聚力＋規則 F 清理＋兩張新圖，要不要下一輪一次做完？**

順序這樣排：

1. **先修第 92 張的 `.r`**（暗色主題看不見，優先）
2. 第 7 輪的兩處（idx 84 莖橫切面小標、idx 88 木質部只修一半＋伴細胞錯開＋篩板壓線）
3. idx 87 內聚力—張力
4. **T2-P3／T2-P4 那兩張「電子與 H⁺ 是兩條路」**——這兩張現在優先度升高了，
   因為課堂上已經有學生問到，而且講義本文剛補完
5. **題目補圖：先做「一張救兩題」的那四組**
6. 舊圖規則 F 清理（21 張）——這個放最後，量大但不影響理解

**2. 玉米那格要不要標子葉（盾片）？**

**不要標。** 你的判斷是對的，理由也對：同色會教錯（盾片是吸收器官不是儲存器官），
異色會讓學生以為兩邊的子葉是不同東西。高中課綱不要求辨認盾片，
在「有胚乳 vs 無胚乳」的對照裡把它畫出來只會製造新的誤解。維持現狀。

**3. 生長素畫法鎖成 `--hi` 小圓點、class 一律 `.aux`。**

同意，之後任何一課畫到生長素分布都沿用這一套。
順帶提醒：這一條也要套到未來的題目附圖上——
`Z10722`／`Z10723` 那組種子發芽率的題目如果要畫，激素的表示法要跟講義一致。
