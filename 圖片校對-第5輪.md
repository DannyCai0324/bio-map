# 圖片校對 · 第 5 輪（bc208e2 驗收）

---

## 一、11 處 ＋ 3 項「可做可不做」：逐項確認過了

| 項目 | 驗到的結果 |
| --- | --- |
| idx 68 易位 | D E 已移到 `x=264/290`、與 X Y 同一列 `y=266`，第二條讀成完整的「X Y D E」；顏色也改回 `bar`（紫），來源看得出來；文字補上「非同源」 ✓ |
| idx 69 互換 | 改成 `A b` ／ `a B` → `A B` ／ `a b`。顏色現在是跟著版本走（大寫紫、小寫綠），所以「組合換了、版本還是 A/a、B/b」在畫面上自己成立，也不再和倒位長得一樣 ✓ |
| idx 70 限制酶 | 切口改成 Z 字：上股一個切點、中間一段水平、下股另一個切點，標籤也改成「限制酶的切口是錯開的」。左圖與右圖現在對得起來 ✓ |
| idx 71 質體 | 第 2 步缺口環 ＋ 第 3 步同半徑紅弧補上缺口，第 4 步菌內小質體同樣處理。兩步終於接得起來 ✓ |
| idx 67 引線 | `M 180,224 L 180,200`，正中第 4 格；插入列前三格已改 `plain`，紅色只留給插入點之後 ✓ |
| idx 66 文字 | 圖內已改「三種置換裡最嚴重」 ✓（但 caption 沒改到，見第三節） |
| idx 73 用語 | 圖內與 caption 都是「基因選殖（DNA 選殖）」 ✓ |
| idx 76 配色 | 已脫離「紅＝錯、綠＝對」的語意 ✓ |
| idx 75 標示 | 補上「RNA 聚合酶」「乳糖（誘導物）與抑制物結合」，抑制物改成向上離開 DNA ✓ |
| idx 72 箭頭 | 循環箭頭已繞到面板外側下方，不再穿過「聚合酶蓋出新股」 ✓ |
| idx 74 圖例 | 改成四個真的基因，第一行「呼吸作用的酵素」三格都亮並加註「三種細胞都要用」 ✓ |

**全域**：78 張、明暗兩色 —— 文字溢出 0、文字互疊 0、禁用寫法 0、非等比縮放 0、
線寬下限 0 違規、viewBox 寬度一律 640、用語與人名零殘留。

---

## 二、又有兩個新缺陷（第三、第四個）——規則 F 抓到的

你說挪了兩次才擺對的那兩處已經好了。但這一版還有**另外兩處新的**，
同樣是修正動作自己長出來的。v2.8 那條規則現在連續兩輪都在生效，這次是我這邊的檢查抓到。

### 1. idx 71：新加的「DNA 連接酶」被第 3 步的質體環壓住

`<text class="n" x="316" y="126">DNA 連接酶</text>` → 字身 **x 316–382.2、y 114.9–128.9**
第 3 步質體環（`path.plas`）圓心約 `(394, 140.75)`、半徑 40；
在那一條字帶上，環的左弧落在 **x 355.8–363.5** ——正好從「接酶」兩個字中間穿過去。

第 2 步環的右緣在 x 294、第 3 步環左緣約 x 356，**中間只有 62 單位**，
而「DNA 連接酶」寬 66.2，一行放不下。

**已驗證的改法**（跑過規則 F，零違規）——拆成兩行置中：

```svg
<text class="n" x="325" y="118" text-anchor="middle">DNA</text>
<text class="n" x="325" y="134" text-anchor="middle">連接酶</text>
```

（也試過只留「連接酶」單行置中 `x=325 y=126`，同樣過關；但 caption 講的是 DNA 連接酶，兩行比較一致。）

### 2. idx 75：新加的「一起離開 DNA」壓在脫落後的抑制物上

`<text class="n" x="258" y="214">一起離開 DNA</text>` → 字身 **x 258–336.2、y 202.9–216.9**
脫落後的抑制物 `path.repr`（`M244,186 h38 v22 h-38 Z`，半透明那個）→ **x 244–282、y 186–208**
重疊在 x 258–282、y 202.9–208，也就是「一起」兩個字騎在方塊的右下角上。

**已驗證的改法**：`x` 從 258 改成 **288**，跟它上面那行「乳糖（誘導物）與抑制物結合」（x=288）對齊。
改完距離抑制物右緣還有 6 單位，兩行也變成整齊的左對齊。

---

## 三、idx 66 的 caption 漏掉了

圖內已經改成「三種置換裡最嚴重」，但 caption 還是原句：

> ……換成終止密碼子、讓蛋白質提早結束，叫無義突變，**通常最嚴重**。

idx 67 的圖內結論是「所以插入／缺失通常比單純置換嚴重」，兩張同課相鄰，
caption 沒改的話那個矛盾其實還在（只是搬到 caption 上）。

**建議**：caption 那句改成「叫無義突變，**是三種置換裡通常最嚴重的**」。

---

## 四、基因選殖那題：你問得對，但要改的是我這邊

**先回答你的問題：不要在 `T4-P6@3` 的 caption 補半句。** 那樣等於用圖去補講義的破口。

我去把講義原文查過了，問題比你看到的更大——**T4-P6 的講義本身就不一致**：

- 心智圖裡本來就寫著「重組 DNA 送入宿主大量複製 → 即**基因選殖**（cloning）」
- 但 block @3 的正文寫「就能做『基因轉殖』（重組 DNA）」
- trap 寫「別和『**基因轉殖**要送進細菌繁殖』搞混」——**這句正是你 idx 73 那張圖在講的事**，
  你把圖改成「基因選殖」之後，圖和它底下的講義正文就對不上了
- 對照表叫「PCR vs 基因轉殖（選殖）」，回顧也寫「基因轉殖：……送入宿主，量產基因或蛋白質」

所以不是你改錯，是我原本就在同一課裡混用兩個詞。**已修 `index.html`**：

1. **@3 正文**：一次把兩個名字定義清楚——
   「把某段 DNA 接上載體、送進宿主，靠宿主大量複製那段 DNA，叫**基因選殖**（gene cloning）；
   如果目的是讓生物表現外來基因、做出帶有新性狀的個體，那叫**基因轉殖**，產物就是基因轉殖生物。」
2. **trap ①**：改成「別和『要送進細菌繁殖的**基因選殖**』搞混」——與你的 idx 73 一致
3. **對照表**：標題改「PCR vs 基因選殖（送進宿主複製）」，欄名改「基因選殖」，
   「主要目的」與「產物」兩列跟著改成複製 DNA 的語意
4. **回顧**：改成「基因選殖：……送入宿主複製；若目的是讓生物表現該基因、產生新性狀，稱基因轉殖」

**不動的部分**（免得你以為漏了）：

- 課程架構裡的章節名「**基因轉殖技術及其應用**」是課本原本的名稱，照留
- 題庫裡 20 處「基因轉殖」我逐一看過前後文，全部是**正確用法**——
  基改鮭魚、GFP 基因轉殖小鼠、抗病作物、藍玫瑰、農桿菌 Ti 質體……
  這些都是「讓生物表現外來基因」，本來就該叫轉殖。不動
- `extend` 那篇把「限制酶、連接酶、PCR、載體與基因轉殖」列為工具箱，也維持原樣

**你只要改一個地方**：`idx 71` 的 caption 開頭「基因轉殖的流程是：」→「**基因選殖的流程是：**」。
那張圖的結論是「細菌每分裂一次，那個基因就多一份」，講的就是複製，用選殖才對得上。
圖內文字（剪下目標基因／剪開質體／接成重組質體／送進細菌繁殖）都是中性的，不用動。

這樣 @3 和 @6 講同一件事時都是「選殖」，而「轉殖」在講義裡只出現一次、有明確定義，
學生連著看不會再問「所以到底是哪個」。

---

## 五、可選（不影響正確性）

`idx 74` 的 caption 還是交響樂團那套比喻（小提琴、法國號、定音鼓），
但圖裡現在是呼吸作用的酵素／肌凝蛋白／神經傳導物／白蛋白。
「總譜、只表現自己那幾行」這個比喻仍然成立，所以不改也可以；
若要對齊，在 caption 末尾加一句「圖裡把『那幾行』換成真的基因來看」就夠。

---

## 六、下一批

T5 植物先推 3 課（P1 組成層次、P2 根莖葉、P3 運輸），沒問題。
上面第二、三、四節的四個小改（idx 71 標籤、idx 75 標籤、idx 66 caption、idx 71 caption）
可以併進那一次推。
